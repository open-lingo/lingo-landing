# Origin of the Open Lingo app (where "Try it free" and "Sign in" point).
VITE_APP_ORIGIN=http://localhost:5173
# Origin this marketing site is served from (canonical URLs, OG tags).
VITE_SITE_ORIGIN=http://localhost:5175
# Optional. Unset falls back to a GitHub privacy issue link.
# VITE_LEGAL_CONTACT_EMAIL=
